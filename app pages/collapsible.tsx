import { Collapsible } from '@/components/ui/collapsible.tsx';

const CollapsiblePage = () => {{
  return (
    <div className="p-4">
      <Collapsible in={false} />
    </div>
  );
}};

export default CollapsiblePage;
