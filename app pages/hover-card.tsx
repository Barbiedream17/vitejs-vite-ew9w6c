import { HoverCard } from '@/components/ui/hover-card.tsx';

const HoverCardPage = () => {{
  return (
    <div className="p-4">
      <HoverCard children={undefined} />
    </div>
  );
}};

export default HoverCardPage;
