import { SectionSkeleton } from '@/components/shared/section-skeleton.tsx';

const SectionSkeletonPage = () => {{
  return (
    <div className="p-4">
      <SectionSkeleton />
    </div>
  );
}};

export default SectionSkeletonPage;
