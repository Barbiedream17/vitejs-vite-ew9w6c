import React from "react";
import { Modal, Box, CloseButton, Text } from "@mantine/core";

export const Dialog = Modal;
export const DialogTrigger = Modal.Target;
export const DialogContent = Modal.Content;
export const DialogHeader = Box;
export const DialogFooter = Box;
export const DialogTitle = Text;
export const DialogDescription = Text;

export const DialogClose = CloseButton;