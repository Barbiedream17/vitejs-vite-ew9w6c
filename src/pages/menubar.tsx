import { Menubar } from '@/components/ui/menubar.tsx';

const MenubarPage = () => {{
  return (
    <div className="p-4">
      <Menubar children={undefined} />
    </div>
  );
}};

export default MenubarPage;
